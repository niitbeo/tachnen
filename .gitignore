# Model files tracked by Git LFS (*.mnn, *.onnx)
# models/  ← đã bỏ ignore, đẩy lên qua LFS
models/.cache/
models/huggingface/

# Python nhúng + thư viện bên thứ ba (150 MB). Cài bằng pip thay vì commit.
python/

# Ảnh kết quả, ảnh thử — có mặt người thật
ket_qua_*
_preview.jpg
*_tachnen.png
*_mask.png
tachnen/

# Rác khi chạy
_commit.txt
__pycache__/
*.pyc
_may.log
_may.err
_thu/

# Frontend build & deps
frontend/node_modules/
frontend/dist/
